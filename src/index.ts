import { add } from './utils/math'

console.log(add)

// const msg = 'hello ts!'

// const dom = document.createElement('div')

// dom.setAttribute('class', 'div')

// document.querySelectorAll('#app')[0].innerHTML = msg
